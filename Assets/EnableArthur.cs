using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnableArthur : MonoBehaviour
{
    public GameObject Arthur;

    // Update is called once per frame
    void Update()
    {
        /*
        if (Wave == 8) {
            Arthur.SetActive(true);
        }
         */
    }
}
